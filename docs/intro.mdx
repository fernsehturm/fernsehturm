---
sidebar_position: 1
---

# Introduction

Fernsehturm (FST) is a local control layer for agent work.

Agents can inspect, draft, run tools, and propose effects. FST decides what is
allowed next by evaluating a process profile, recorded artifacts, approvals,
and the current run state.

```text
agent intent -> FST profile gate -> route -> evidence -> controlled effect
```

The MVP is local-first:

```text
one fst Linux binary
local_file or sqlite store
versioned workspace config
installable process packs
hook-backed process logic
scenario runner
replayable evidence
local MCP controller
```

## What FST Controls

FST answers these questions for an agent:

- Is this action part of the active process profile?
- Which gate applies before the action?
- Which artifacts or approvals are missing?
- Should the agent continue, ask, repair, wait, or stop?
- May this effect be mocked, shadowed, or materialized?
- What evidence should be recorded for replay?

The agent stays useful. It does not become the authority.

## What You Do

You install `fst`, initialize a workspace, install or activate a process pack,
run scenarios, and connect your agent through the local MCP controller.

For the MVP, the main process pack is `local_patch_review`: it controls when a
local patch may be marked ready for review.

## Start Here

- [Getting Started](getting-started/overview.md): install and run the local MVP
- [How FST Works](understanding/how-it-works.md): the runtime control loop
- [Developing A Process](workflows/developing-a-process.md): how to create process profiles
- [Process Builder Agent Guide](workflows/process-builder-agent.md): how an agent helps build profiles
- [API Reference](concepts/04_api.md): `fst.control` request and response shape
